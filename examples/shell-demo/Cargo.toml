[package]
name = "shell-demo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shell demo (tray + app menu + context menu)"

[dependencies]
makepad-components = { workspace = true, features = ["Modal"] }
makepad-widgets    = { workspace = true }
